@model WebSiteASP.Models.Service
@{
    ViewBag.Title = Model.Name;
}

<div class="linkDiv">
    <a href="@Url.Action("Index", "Home")">Главная ></a>
    <a href="@Url.Action("Services", "Home")"> Услуги ></a>
    <a href="#"> @Model.Name</a>
</div>

    <div class="thisServiceDiv">
        <h2>@Model.Name</h2>

        @MvcHtmlString.Create(Model.Description)
    </div>